class BitcoinWallet{
  String privateKey;
  String publicKey;
  String address;

  BitcoinWallet({
    required this.privateKey,
    required this.publicKey,
    required this.address
  });
}